schema_version: "1.0"
staging_root: /absolute/path/to/camera-system-runtime/staging
server_url: http://192.168.1.100:8000
# 仅为旧部署兼容；Jetson 客户端和查看器不使用 WSL 文件路径。
remote_task_root: /absolute/path/on/wsl/rx_tasks
upload_timeout_seconds: 300
result_root: /absolute/path/to/camera-system-results
result_layout: capture_flat
request_timeout_seconds: 300
status_poll_interval_seconds: 2
reconstruction_timeout_seconds: 21600
download_timeout_seconds: 600
max_ply_size_bytes: 2147483648
